# FastAPI & server
fastapi==0.110.0
uvicorn[standard]==0.29.0

# Database & ORM async
sqlalchemy[asyncio]==2.0.30
asyncpg==0.28.0

# Optional: for Alembic migrations (can stay sync)
alembic==1.13.1

# Authentication & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1    # 🔹 IMPORTANTE: versión compatible con passlib

# Request parsing & form handling
python-multipart==0.0.9

# Rate limiting
slowapi==0.1.9

# HTTP client
httpx==0.27.0

# Cloudinary
cloudinary==1.40.0

# Pydantic
pydantic==2.6.4
pydantic-settings==2.2.1

# ⚠ Omitimos psycopg2-binary, ya que asyncpg es el driver async recomendado
# psycopg2-binary==2.9.7   # ❌ No necesario para asyncpg
